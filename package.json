{
  "name": "ronbun",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "db:migrate:local": "wrangler d1 migrations apply arxiv-db --local --config apps/api/wrangler.toml",
    "db:migrate:remote": "wrangler d1 migrations apply arxiv-db --remote --config apps/api/wrangler.toml"
  },
  "packageManager": "bun@1.3.8",
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "turbo": "^2.8.3",
    "typescript": "^5.8.3"
  }
}
