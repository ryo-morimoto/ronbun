{
  "name": "ronbun",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "prepare": "lefthook install",
    "db:migrate:local": "wrangler d1 migrations apply ronbun-db --local --config apps/api/wrangler.toml",
    "db:migrate:preview": "wrangler d1 migrations apply ronbun-db-preview --env preview --remote --config apps/api/wrangler.toml",
    "db:migrate:production": "wrangler d1 migrations apply ronbun-db --env production --remote --config apps/api/wrangler.toml"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "lefthook": "^2.1.0",
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0",
    "turbo": "^2.8.3",
    "typescript": "^5.8.3"
  },
  "packageManager": "bun@1.3.8"
}
